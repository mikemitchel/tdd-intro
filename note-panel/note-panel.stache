<can-import from="~/note-panel/note-panel.css"></can-import>

    <ul>
      {{#for (note of this.notesList)}}
        <li
          on:click="setActive(note)"
          class="{{#if(note.isActive)}}active{{/if}}">
          {{note.id}}: {{note.title}}
        </li>
      {{/for}}
    </ul>
